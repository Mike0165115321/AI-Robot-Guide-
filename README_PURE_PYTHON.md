# 🤖 AI Robot Guide จังหวัดน่าน (v1.0.0 Hybrid Edition)

โปรเจกต์ AI Guide แนะนำสถานที่ท่องเที่ยวและข้อมูลสำคัญของจังหวัดน่าน พัฒนาด้วยสถาปัตยกรรม Hybrid (Python/Go/C++) เพื่อประสิทธิภาพสูงสุด

## 📚 เอกสารสำคัญ (Documentation)
* [📖 คู่มือการติดตั้งและเตรียม Environment แบบละเอียด (Installation Guide)](docs/INSTALLATION.md)
* [🏗️ โครงสร้างระบบและแผนพัฒนา (Architecture & Roadmap)](docs/ARCHITECTURE_AND_ROADMAP.md)

## ✨ คุณสมบัติหลัก (Features)

* **Chat Interface:** หน้าแชทสำหรับพูดคุย (Text & Voice) รองรับการแสดงผลแผ่นที่และรูปภาพแบบ In-Place
* **Avatar Interface:** หน้าหุ่นยนต์ Avatar (3D) ที่โต้ตอบได้ พร้อมระบบ **Wake Word** ("น้องน่าน") และท่าทางประกอบ
* **Streaming TTS (New!):** ระบบเสียงพูดแบบ Streaming (Sentence-based) พูดทันทีไม่ต้องรอจบประโยค เร็วและลื่นไหล
* [📖 อ่านแผนพัฒนาและโครงสร้างระบบฉบับเต็ม (Architecture & Roadmap)](docs/ARCHITECTURE_AND_ROADMAP.md)
* **FabManager Tools:** เครื่องมืออำนวยความสะดวกแบบ Pop-up (Music, FAQ, Calculator, Navigation) ใช้งานได้ทั้งหน้า Chat และ Avatar
* **Admin Panel:** หน้าจัดการข้อมูลหลังบ้าน
  * 📊 **Google Sheets Sync:** เชื่อมต่อได้ทั้งแบบ **Public Mode** (ไม่ต้องใช้ Key) และ **Service Account** พร้อมระบบ **Real-time Webhook**
  * ⚙️ **Dynamic Table:** ปรับแต่งตารางข้อมูลได้อิสระ
* **RAG Pipeline:** ระบบค้นหาข้อมูลอัจฉริยะจาก Vector DB (Qdrant) แม่นยำเรื่องสถานที่ท่องเที่ยวเมืองน่าน
* **Multi-LLM & AI Modes:**
  * รองรับทั้ง Gemini และ Groq
  * **AI Mode Selector:** เลือกโหมด "คิดเร็ว" (Fast) หรือ "คิดละเอียด" (Detailed) ได้
* **Advanced Speech:**
  * **STT:** Whisper (Groq/Local) แม่นยำสูง
  * **TTS:** EdgeTTS (เสียงธรรมชาติ) + gTTS Fallback (Speed up 1.25x) เมื่อเกิดปัญหา
* 🌐 **Multi-language Support:** รองรับการสื่อสาร **7 ภาษา** (ไทย, อังกฤษ, จีน, ญี่ปุ่น, รัสเซีย, อินเดีย, มาเลย์) ทั้งรูปแบบ Text และ Voice

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
# AI Robot Guide (Python Only)

ระบบ AI สำหรับหุ่นยนต์แนะนำการท่องเที่ยวจังหวัดน่าน
ทำงานด้วย **Python (FastAPI)** ทั้งหมด เพื่อความเสถียรและดูแลรักษาง่าย

## 🏗 Architecture (Pure Python High-Performance)
- **Backend:** Python (FastAPI) [Port 9090]
  - Handles API, RAG, Chat Logic
  - **Faster-Whisper Integration** (High Speed STT)
  - Serves Frontend Static Files
- **Database:** MongoDB & Qdrant (Docker)

## 🚀 Quick Start
### 1. Prerequisites
- **Docker Desktop** (for Databases)
- **Python 3.11+**
- **FFmpeg** (Required for Audio)

### 2. Setup
```bash
# Clone
git clone https://github.com/Mike0165115321/AI-Robot-Guide-.git
cd AI-Robot-Guide-

# Start System (Databases + Python Backend)
./start_all.sh
```

### 3. Usage
Access the application at: **http://localhost:9090**

## 🔮 Future Roadmap (Advanced Architecture)
We have documented a **High-Performance Hybrid Architecture (Go Gateway + C++ Audio Engine)** plan for future scaling. This design offers superior concurrency and lower resource usage for STT.

📌 Read the full plan here: [Future Architecture Plan](docs/FUTURE_ARCHITECTURE_PLAN.md)

* **Frontend:** HTML5, CSS3, JavaScript (Vanilla JS)
* **Backend (Logic):** Python FastAPI (Port 9090)
* **Database (Dockerized):**
    * MongoDB (สำหรับเก็บข้อมูลหลัก)
    * Qdrant (สำหรับ Vector Search)
* **AI / LLM:** Gemini, Groq (Llama 3.x), LangChain/LlamaIndex
* **Speech:** EdgeTTS (Streaming), gTTS

---

## 🚀 วิธีการติดตั้งและรันโปรเจกต์ (Setup & Run)

### 1. วิธีแบบด่วน (Recommended)
ใช้สคริปต์ `start_all.sh` เพื่อรันทุกบริการพร้อมกัน (Database, Audio Engine, Python Backend, Go Gateway)

```bash
chmod +x start_all.sh
./start_all.sh
```
จากนั้นเข้าใช้งานได้ที่: `http://localhost:8080`

---

### 2. การติดตั้งแบบ Manual (ทีละขั้นตอน)

#### 2.1 ติดตั้งโปรแกรมที่จำเป็น (Prerequisites)
ก่อนเริ่ม ตรวจสอบว่าเครื่องของเรามีโปรแกรมเหล่านี้ติดตั้งอยู่:
* [Python](https://www.python.org/) (แนะนำเวอร์ชัน 3.12 ขึ้นไป)
* [Docker Desktop](https://www.docker.com/products/docker-desktop/) (สำหรับรัน MongoDB และ Qdrant)
* [Go](https://go.dev/) (ทดสอบด้วย version **1.22.2**)
* [CMake](https://cmake.org/) (**3.28.3**) และ **GCC** (**13.3.0** สำหรับ C++ Audio Engine)

#### 2.2 โคลนโปรเจกต์ (Clone)
(ข้ามขั้นตอนนี้ไป ถ้าเราได้ไฟล์โปรเจกต์มาแล้ว)

```
git clone [https://github.com/Mike0165115321/AI-Robot-Guide-.git](https://github.com/Mike0165115321/AI-Robot-Guide-.git)
cd AI-Robot-Guide-
```

#### 2.3 รันฐานข้อมูล (Run Databases)

วิธีที่ง่ายที่สุดในการรันฐานข้อมูล (MongoDB และ Qdrant) คือการใช้ Docker Compose

1.  ตรวจสอบว่า Docker Desktop กำลังทำงานอยู่ (Running)
2.  เปิด Terminal ที่โฟลเดอร์หลักของโปรเจกต์ (ที่มีไฟล์ `docker-compose.yml` อยู่)
3.  รันคำสั่ง:

```
# คำสั่งนี้จะสร้างและรัน MongoDB (ที่ port 27017)
# และ Qdrant (ที่ port 6333) ให้เราอัตโนมัติ
docker-compose up -d
(ถ้าต้องการหยุดฐานข้อมูล ให้ใช้ docker-compose down)
```
#### 2.4 ตั้งค่า Python Virtual Environment (venv)
```
# สร้าง venv (ใช้ชื่อ .venv-robot เพื่อความชัดเจน)
python -m venv .venv-robot
```

### Activate venv
### บน Linux
```
source .venv-robot/bin/activate
```

#### 2.5 ติดตั้ง Dependencies(ไลบารี่ทั้งหมด)
```
# ตรวจสอบว่าอยู่ใน venv แล้ว
# (สังเกตว่ามี (.venv-robot) นำหน้า prompt)
pip install -r python-backend/requirements.txt
```

#### 2.6 ‼️ ตั้งค่า Environment (สำคัญที่สุด!)
โปรเจกต์นี้ต้องใช้ API Keys หลายตัวในการทำงาน
ไปที่โฟลเดอร์ python-backend/
สร้างไฟล์ใหม่ ชื่อว่า .env
เปิดไฟล์ python-backend/เอาคีย์มาใส่ สร้าง .env.txt เพื่อดู "คู่มือ" ว่าต้องใช้ Key อะไรบ้าง และไปหามาจากที่ไหน
คัดลอก Key ทั้งหมดมาวางในไฟล์ .env ที่เพิ่งสร้าง
สำคัญ: ค่า MONGO_URI และ QDRANT_HOST ในไฟล์ .env (หรือใน config.py default) ควรเป็น localhost หรือ 127.0.0.1 เพื่อให้มันเชื่อมต่อกับ Docker ที่รันในเครื่องเราได้

#### 2.7 สร้างฐานข้อมูล Vector (Build Vectors)
ก่อนรันเซิร์ฟเวอร์ครั้งแรก เราต้องนำข้อมูล .jsonl ไปประมวลผลและเก็บใน Qdrant ก่อน
# ตรวจสอบว่ายังอยู่ใน venv-robot
7.1
```
# รันสคริปต์เพื่อ ทำให้ฐานข้อมูลรู้จักภาพ
python3 python-backend/utils/add_image_links.py
```

7.2
```
# รันสคริปต์เพื่อสร้าง Vector Database
python3 python-backend/scripts/build_vectors.py
```
(หากมีข้อผิดพลาด ตรวจสอบว่า Docker (MongoDB/Qdrant) กำลังทำงานอยู่หรือไม่)

### อ่านก่อน!!!!! ห้ามรันมั่ว
```
# หากฐานข้อมูลมีปัญหา ให้รันไฟล์ เพื่อลบข้อมูลในฐานข้อมูล แล้วกลับไปทำ 7.1 ได้
python3 python-backend/scripts/clear_database.py
```

#### 2.8 รันเซิร์ฟเวอร์ (Run Services)

**Python Backend:**
```bash
cd python-backend
uvicorn api.main:app --host 127.0.0.1 --port 9090 --reload
```

**Go Gateway:**
```bash
cd go-gateway
go run cmd/server/main.go
```

**C++ Audio Engine:**
(ต้อง Build ก่อน ดูใน docs/CPP_AUDIO_ENGINE.md)
```bash
cd cpp-audio-engine
./build/audio-engine --port 50051
```
