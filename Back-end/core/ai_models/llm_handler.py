# /core/ai_models/llm_handler.py (V3 - Full Groq Integration)

import google.generativeai as genai # (เราจะลบทีหลัง แต่ตอนนี้เก็บไว้ก่อน)
from groq import AsyncGroq 
import logging 
import asyncio 
from typing import Dict, Any, Optional

from core.config import settings
from .key_manager import gemini_key_manager, groq_key_manager 

async def get_llama_response_direct_async(user_query: str) -> str:
    api_key = groq_key_manager.get_key()
    if not api_key: 
        logging.error("[Small Talk] No Groq API key available.")
        return "ขออภัยค่ะ มีปัญหาในการเชื่อมต่อค่ะ"
        
    try:
        client = AsyncGroq(api_key=api_key)
        
        system_prompt_small_talk = """You are 'Nong Nan', a cheerful AI tour guide for Nan province, Thailand. Your task is to engage in simple, positive small talk.
- Keep responses very short (1-2 sentences).
- Always be friendly and polite.
- If appropriate, end with a gentle question to keep the conversation going.

Example:
User: สวัสดี
Your response: สวัสดีค่ะ! ยินดีที่ได้คุยกันนะคะ มีอะไรให้น้องน่านช่วยไหมคะ?
"""
        
        logging.info(f"💬 [Small Talk] Handling query with Llama-8B (Async): '{user_query}'")
        
        chat_completion = await client.chat.completions.create(
            messages=[
                {"role": "system", "content": system_prompt_small_talk},
                {"role": "user", "content": user_query}
            ],
            model="llama-3.1-8b-instant",
            temperature=0.7, 
            max_tokens=100
        )
        response_text = chat_completion.choices[0].message.content
        logging.info("✅ [Small Talk] Received direct response from Groq (Async).")
        return response_text
        
    except Exception as e:
        logging.error(f"❌ [Small Talk] Error calling Groq API (Async): {e}", exc_info=True)
        return "ขออภัยค่ะ มีปัญหานิดหน่อยค่ะ"

def get_insights_from_logs(log_collection) -> dict:
    if log_collection is None:
        return {}
    
    try:
        top_topics_cursor = log_collection.aggregate([
            {"$group": {"_id": "$primary_topic", "count": {"$sum": 1}}},
            {"$sort": {"count": -1}},
            {"$limit": 3}
        ])
        top_topics = [item["_id"] for item in top_topics_cursor if item.get("_id")]
        
        if top_topics:
            logging.info(f"📈 [Analytics] Top topics found: {top_topics}")
            return {"top_topics": top_topics}
            
        return {}
    except Exception as e:
        logging.error(f"❌ [Analytics] Failed to get insights: {e}", exc_info=True)
        return {}


def _generate_llama_rag_prompts(user_query: str, context: str, insights: dict) -> Dict[str, str]:
    insights_text = "ตอนนี้ยังไม่มีข้อมูลเชิงลึกค่ะ"
    if top_topics := insights.get("top_topics"):
        top_topics_str = ", ".join(top_topics)
        insights_text = f"ข้อมูลล่าสุด: สถานที่ที่นักท่องเที่ยวคนอื่นๆ ถามถึงบ่อยที่สุดคือ {top_topics_str}"
    
    system_prompt = f"""# ภารกิจ
คุณคือ "น้องน่าน" ไกด์ท้องถิ่นผู้เชี่ยวชาญประจำจังหวัดน่านที่มีนิสัยร่าเริง เป็นมิตร และให้ข้อมูลได้อย่างยอดเยี่ยม
เป้าหมายของคุณคือการสร้างคำตอบที่ครบถ้วน มีโครงสร้างสวยงาม และน่าสนใจให้กับนักท่องเที่ยว โดย **ต้อง** ใช้ "ข้อมูลประกอบ" (Context) ที่จะให้มาเท่านั้น

# กฎการจัดรูปแบบคำตอบ
1.  **บุคลิกและน้ำเสียง:** รักษาน้ำเสียงที่อบอุ่น เป็นกันเอง ใช้สรรพนามแทนตัวเองว่า "น้องน่าน" และลงท้ายด้วย "ค่ะ" อย่างเป็นธรรมชาติ
2.  **การใช้ข้อมูล:** คำตอบของคุณ **ต้อง** อ้างอิงจาก "ข้อมูลประกอบ" เท่านั้น ห้ามแต่งข้อมูลหรือใช้ความรู้ภายนอกเด็ดขาด
3.  **โครงสร้าง:** หากนักท่องเที่ยวถามคำถามกว้างๆ ให้จัดกลุ่มคำตอบตามหมวดหมู่ แต่ **จงสร้างหัวข้อหมวดหมู่ (`### ...`) ก็ต่อเมื่อมีข้อมูลที่เกี่ยวข้องใน "ข้อมูลประกอบ" เท่านั้น** หากไม่มีข้อมูลสำหรับหมวดหมู่ใดเลย **ห้าม** สร้างหัวข้อของหมวดหมู่นั้นขึ้นมาเด็ดขาด หมวดหมู่ที่มีให้ใช้คือ `### 🏞️ สายธรรมชาติและวิวทิวทัศน์`, `### 🙏 สายวัดและประวัติศาสตร์`, `### 🍜 สายกินและวัฒนธรรม`
4.  **รายละเอียดสถานที่:** สำหรับแต่ละสถานที่ ให้ใช้รูปแบบ: รายการตัวเลข, ชื่อ **ตัวหนา**, และคำอธิบายแบบจุด (`*`)
5.  **การใช้ Markdown:** ใช้ **ตัวหนา** (`**ข้อความ**`) กับชื่อและจุดเด่นที่สำคัญ
6.  **กรณีไม่ทราบข้อมูล:** หาก **"ข้อมูลประกอบ" ทั้งหมด** ไม่มีเนื้อหาที่ตอบคำถามได้เลย ให้ตอบว่า: "ขออภัยค่ะ น้องน่านยังไม่มีข้อมูลเกี่ยวกับเรื่องนี้ในระบบเลยค่ะ"
7.  **การปิดท้าย:** จบด้วยการเชิญชวนให้ถามคำถามเพิ่มเติมเสมอ
8.  **ข้อมูลเชิงลึก:** {insights_text} (คุณสามารถใช้ข้อมูลนี้สร้างบทสนทนาได้ เช่น "โอ้! วัดภูมินทร์เหรอคะ เป็นที่ที่นักท่องเที่ยวคนอื่นๆ ถามถึงบ่อยที่สุดเลยค่ะ!")

จงตอบ "คำถามของนักท่องเที่ยว" โดยใช้ "ข้อมูลประกอบ" ที่จะให้มาใน user message
"""

    user_prompt = f"""# ข้อมูลประกอบ (Context)
---
{context}
---

# คำถามของนักท่องเที่ยว
{user_query}

# คำตอบของคุณ (ในฐานะ 'น้องน่าน'):
"""
    
    return {"system": system_prompt.strip(), "user": user_prompt.strip()}


async def get_groq_rag_response_async(user_query: str, context: str, insights: dict) -> str:
    api_key = groq_key_manager.get_key()
    if not api_key:
        logging.error("[LLM-RAG] No Groq API key available.")
        return "เกิดข้อผิดพลาด: ไม่ได้ตั้งค่า Groq API Key"
        
    try:
        prompts = _generate_llama_rag_prompts(user_query, context, insights)
        
        client = AsyncGroq(api_key=api_key)
        
        logging.info(f"🤖 [LLM-RAG] Calling Groq RAG API (Async) using Llama 70B...")
        
        chat_completion = await client.chat.completions.create(
            messages=[
                {"role": "system", "content": prompts["system"]},
                {"role": "user", "content": prompts["user"]}
            ],
            model=settings.GROQ_LLAMA_MODEL, 
            temperature=0.3, 
            max_tokens=4096 
        )
        
        response_text = chat_completion.choices[0].message.content
        logging.info("✅ [LLM-RAG] Received response from Groq 70B (Async).")
        
        if not response_text:
            logging.warning("[LLM-RAG] Groq 70B response was empty.")
            return "ขออภัยค่ะ มีปัญหาในการสร้างคำตอบ ลองถามใหม่อีกครั้งนะคะ"

        return response_text
        
    except Exception as e:
        logging.error(f"❌ [LLM-RAG] Error calling Groq 70B API (Async): {e}", exc_info=True)
        return "ขออภัยค่ะ เกิดข้อผิดพลาดในการเชื่อมต่อกับระบบ AI หลัก"
