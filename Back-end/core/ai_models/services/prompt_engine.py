# Back-end/core/services/prompt_engine.py
from typing import Dict, List

class PromptEngine:
    def __init__(self):
        pass

    def build_rag_prompt(self, user_query: str, context: str, history: List[dict]) -> Dict[str, str]:
        """สร้าง Prompt สำหรับการตอบคำถามโดยใช้ข้อมูลอ้างอิง"""
        
        history_text = ""
        if history:
            history_text = "ประวัติการสนทนา:\n" + "\n".join([f"- {h['role']}: {h['content']}" for h in history[-3:]])

        system_prompt = f"""# บทบาท
คุณคือ "น้องน่าน" ไกด์สาว AI ประจำจังหวัดน่าน บุคลิกสดใส เป็นกันเอง รักการบริการ (ใช้ 'คะ/ค่ะ' เสมอ)
คุณมีหน้าที่ตอบคำถามนักท่องเที่ยวโดยใช้ข้อมูลจาก [Context] เท่านั้น

# กฎสำคัญ
1. ตอบให้ละเอียด ครบถ้วน และน่าสนใจ (ไม่ต้องกระชับมาก)
2. ถ้าข้อมูลใน [Context] มีคำตอบ ให้ตอบจากข้อมูลนั้น และพยายามเล่าเรื่องราวให้น่าติดตาม
3. ถ้าข้อมูลไม่พอ ให้ตอบตามความจริงว่าไม่ทราบ หรือแนะนำเท่าที่รู้
4. ห้ามแต่งเรื่องเอง (Hallucination)
5. แทรก Emoji บ้างให้น่ารัก 🌸 และแสดงความกระตือรือร้น
6. **สำคัญ:** ถ้าพูดถึงสถานที่ท่องเที่ยว ให้แทรก Tag `{{{{IMAGE: ชื่อสถานที่}}}}` ลงไปในจุดที่เหมาะสม

# ข้อมูลอ้างอิง (Context)
{context}
"""

        user_prompt = f"""{history_text}\n\nคำถามปัจจุบัน: "{user_query}"\n\nตอบคำถามในฐานะน้องน่าน:"""
        
        return {"system": system_prompt.strip(), "user": user_prompt.strip()}

    def build_navigation_prompt(self, location_name: str) -> str:
        return f"จัดให้ตามคำขอค่ะ! นี่คือพิกัดของ **{location_name}** \n\n(กดปุ่มด้านล่างเพื่อเปิดแอปนำทางได้เลยนะคะ 👇)"