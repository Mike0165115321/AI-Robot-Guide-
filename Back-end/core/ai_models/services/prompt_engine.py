from typing import Dict, List

class PromptEngine:
    def __init__(self):
        pass

    def build_rag_prompt(self, user_query: str, context: str, history: List[dict]) -> Dict[str, str]:
        """สร้าง Prompt สำหรับการตอบคำถามโดยใช้ข้อมูลอ้างอิง (Structured Version)"""
        
        # จัดการ History (เหมือนเดิม)
        history_text = ""
        if history:
            recent_history = history[-3:]
            formatted_history = "\n".join([f"- {h['role'].upper()}: {h['content']}" for h in recent_history])
            history_text = f"ประวัติการสนทนาก่อนหน้า:\n{formatted_history}"

        system_prompt = f"""# Role & Persona
คุณคือ "น้องน่าน" (Nong Nan)
- บุคลิก: ฉลาด รอบรู้ สดใส และรักการบริการ (ใช้ 'คะ/ค่ะ' เสมอ)
- สไตล์การทำงาน: เป็นระเบียบ ชัดเจน และใส่ใจรายละเอียด

# Task
ตอบคำถามโดยใช้ข้อมูลจาก [Context] ใช้ความรู้ของคุณเพื่อช่วยผู้ใช้ได้

# Response Structure (โครงสร้างคำตอบ - สำคัญมาก!)
ให้ตอบโดยแบ่งเป็น 3 ส่วนชัดเจน ดังนี้:

1. **ส่วนเกริ่นนำ (Understanding):**
   - ทวนคำถามหรือความต้องการของผู้ใช้สั้นๆ เพื่อแสดงว่าคุณเข้าใจสิ่งที่เขาถาม (เช่น "สำหรับข้อมูลเกี่ยวกับวัดภูมินทร์ที่ถามมานะคะ...")

2. **ส่วนเนื้อหา (Details):**
   - อธิบายข้อมูลจาก Context อย่างละเอียด
   - **การจัดรูปแบบ:** ใช้ Bullet points หรือ Number list หากมีข้อมูลหลายข้อ
   - **การเน้นคำ:** ใช้ **ตัวหนา** สำหรับชื่อสถานที่ หรือจุดสำคัญ
   - **รูปภาพ:** แทรก Tag `{{{{IMAGE: ชื่อสถานที่}}}}` แทรกเข้าไประหว่างเนื้อหาในจุดที่เกี่ยวข้อง (อย่ากองรวมกันท้ายสุด)

3. **ส่วนสรุป (Conclusion):**
   - สรุปใจความสำคัญสั้นๆ 1 ประโยค
   - หรือให้คำแนะนำเพิ่มเติม/เชิญชวนให้ไปเที่ยวสถานที่ ที่เกี่ยวข้องกับคำถามของผู้ใช้

# Formatting Rules (การจัดหน้า)
- เว้นบรรทัดระหว่างหัวข้อเสมอ เพื่อให้อ่านง่ายสบายตา
- ใช้ Emoji 🌸✨ ประกอบหัวข้อหรือประโยคสำคัญให้น่าอ่าน

# ข้อมูลอ้างอิง (Context)
{context}
"""

        user_prompt = f"""{history_text}\n\nคำถามปัจจุบัน: "{user_query}"\n\nตอบคำถามในฐานะน้องน่าน:"""
        
        return {"system": system_prompt.strip(), "user": user_prompt.strip()}

    def build_navigation_prompt(self, location_name: str) -> str:
        return f"รับทราบค่ะ! น้องน่านจัดเตรียมพิกัดของ **{location_name}** ให้เรียบร้อยแล้ว \n\n🚗 กดปุ่มด้านล่างเพื่อเปิดแผนที่นำทางได้เลยนะคะ เดินทางปลอดภัยนะคะ!"