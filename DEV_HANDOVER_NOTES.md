# 🤖 AI Robot Guide - Handover Notes

**สถานะล่าสุด:** 2026-01-09 (เสถียร / Stable Build)

## 📜 System Design Principles (กฎเหล็กการออกแบบ)
เรายึดถือหลักการต่อไปนี้อย่างเคร่งครัดในการพัฒนา หากมีโค้ดส่วนไหนขัดแย้ง ต้องหาทาง Refactor ทันทีเมื่อมีโอกาส:

1.  **Separation of Concerns (SoC):** แยกส่วน Logic, Data, และ Interface ออกจากกัน "ห้องครัวไม่ปนห้องนอน"
2.  **Modularity:** โค้ดต้องเป็นจิ๊กซอว์ (Module) ที่หยิบไปใช้ซ้ำได้ ไม่ผูกติดกันจนแกะไม่ออก
3.  **Single Responsibility Principle (SRP):** 1 ไฟล์/คลาส รับผิดชอบเพียง 1 เรื่องเท่านั้น
4.  **Hardware Abstraction Layer (HAL):** แยกส่วนติดต่ออุปกรณ์ (Mic/Speaker/Sensors) ออกจาก Logic หลัก

> **The Golden Prompt:**
> *"ช่วยเขียนโค้ดโดยใช้แนวคิด Separation of Concerns (SoC) และ Modular Programming นะครับ ผมต้องการให้แยก Logic, Data, และ Interface ออกจากกันอย่างชัดเจน โดยให้แต่ละส่วนเก็บเป็นไฟล์เฉพาะทาง (SRP) และใช้ตัวแปรในการส่งผ่านข้อมูล เพื่อให้ง่ายต่อการดูแลรักษาและขยายระบบในอนาคต"*

---

## ✅ งานที่ทำเสร็จแล้ว
1.  **Refactoring Phase (Phase 5 Complete):** 🏗️
    *   **Frontend Modularization:** แยก `app.js` ออกเป็น `StateManager.js` (คุม Data) และ `UIManager.js` (คุมหน้าจอ) ตรงตามหลัก **SoC/SRP**
    *   **Frontend Director:** แยกสมองตัดสินใจหน้าบ้าน ออกจาก Service อื่นๆ
    *   **Eliminate Redundancy:** แก้ปัญหาส่งคำถามซ้ำซ้อน และจัดการสไลด์ (Slide Visibility) ด้วยระบบ **Key (show_slide)**

2.  **Frontline Gatekeeper "เลขา" (Google Assistant):**
    *   Fast-Track ดีเลย์ต่ำ (0.1s)
    *   แยก Role ชัดเจน: เลขา (Google) vs ผู้เชี่ยวชาญ (RAG)
    *   **Test Suite:** มี Script `test_director_mock.js` สำหรับทดสอบพฤติกรรม

3.  **Infrastructure:**
    *   `register_device.py`: ลงทะเบียนสำเร็จ
    *   `system_health_check.py`: ตรวจสุขภาพระบบได้

## 🚧 สิ่งที่ต้องทำต่อ (Phase 6: Stability & Hardening) - "รัดกุม" 🛡️
1.  **Error Handling & Edge Cases:**
    *   ถ้า Google หลับ? (Timeout Logic)
    *   ถ้า RAG ตอบช้าเกิน 10 วิ? (Progress Feedback)
    *   ถ้าเน็ตหลุดตอนพูด? (Offline Detection)
2.  **Performance Tune-up:** เช็ค Memory Leak ของ Browser หลังเปิดทิ้งไว้นานๆ
3.  **Field Test:** ทดสอบบน Hardware จริง (Microphone/Touchscreen)

## 📌 ไฟล์สำคัญที่แก้ไขล่าสุด
*   `frontend/js/app.js`: (New) Controller Logic ที่สะอาดขึ้น
*   `frontend/js/modules/StateManager.js`: (New) ศูนย์รวมตัวแปร
*   `frontend/js/modules/UIManager.js`: (New) ศูนย์รวมหน้าจอ
*   `frontend/js/services/frontendDirector.js`: สมองหน้าบ้าน
*   `Back-end/api/schemas.py`: เพิ่ม Field `show_slide`

---
*บันทึกโดย: Antigravity Agent*
