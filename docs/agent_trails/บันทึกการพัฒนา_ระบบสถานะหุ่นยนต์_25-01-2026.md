# บันทึกการทำงาน (Agent Trails): การพัฒนาระบบสถานะหุ่นยนต์บน Web UI

**วันที่:** 25-01-2026
**งาน:** รวมระบบแสดงสถานะหุ่นยนต์ (Robot Status) เข้ากับ Web UI

## การตัดสินใจสำคัญ

### 1. การใช้ File-based Communication แทน Direct IPC
- **เหตุผล:** `ros2_bridge.py` รันอยู่ในสภาพแวดล้อมของ ROS 2 (Python 3.10+) ในขณะที่ระบบ Backend (FastAPI) รันแยกโปรเซสกัน การสื่อสารผ่าน Inter-Process Communication (IPC) โดยตรงอาจมีความซับซ้อนในการตั้งค่า
- **วิธีการ:** ให้ `ros2_bridge.py` เขียนสถานะ Heartbeat ลงในไฟล์ `/tmp/robot_heartbeat.json` ทุกๆ 1 วินาที
- **ข้อดี/ข้อเสีย:** แม้จะเพิ่ม Latency เล็กน้อย แต่ช่วยให้ Components ต่างๆ แยกจากกันได้อย่างอิสระ (Decoupled) และง่ายต่อการ Debug

### 2. การตั้งค่า Heartbeat Timeout ไว้ที่ 3 วินาที
- **เหตุผล:** ESP32 ถูกโปรแกรมให้ Publish ข้อมูล Feedback กลับมาทุกๆ Loop (ประมาณ 50ms) ดังนั้นหากข้อมูลหายไปเกิน 3 วินาที ถือว่าเพียงพอที่จะระบุได้ว่าเกิดการตัดการเชื่อมต่อ (Disconnect)
- **ทางเลือก:** สามารถปรับเป็น 5 วินาทีได้หากพบว่าเครือข่ายไม่มีความเสถียร (Unstable Network)

### 3. การ Polling API ทุกๆ 2 วินาทีบนหน้า Web UI
- **เหตุผล:** เป็นค่าที่สมดุลระหว่างความลื่นไหลในการแสดงผล (Responsiveness) และภาระงานของ Server (Server Load)
- **ทางเลือกเพิ่มเติม:** ในอนาคตหากต้องการความเร็วระดับ Real-time มากขึ้น สามารถเปลี่ยนไปใช้ JavaScript WebSocket ได้

## ไฟล์ที่เกี่ยวข้องและการแก้ไข
- `Back-end/core/hardware/ros2_bridge.py` - เพิ่มส่วน Heartbeat subscriber และตัวเขียนไฟล์สถานะ
- `Back-end/api/routers/hardware_api.py` - สร้าง API endpoint `/robot-status` เพื่ออ่านสถานะจากไฟล์
- `frontend/admin/hardware_test.html` - เพิ่ม HTML/JS สำหรับแสดงผล Traffic Light (ไฟสถานะ)
- `frontend/admin/assets/styles/hardware.css` - เพิ่ม CSS สำหรับ Traffic Light และแอนิเมชันกะพริบ

## ผลลัพธ์ที่ได้
ผู้ดูแลระบบสามารถทราบสถานะของหุ่นยนต์ได้ทันทีจากหน้าจอควบคุม โดยไม่ต้องตรวจสอบจากตัวหุ่นยนต์โดยตรง:
- 🔴 **Stopped:** ระบบปิดอยู่
- 🟡 **Connecting:** กำลังพยายามเชื่อมต่อ (รอ Heartbeat)
- 🟢 **Ready:** เชื่อมต่อสำเร็จ พร้อมควบคุม
