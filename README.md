# 🤖 AI Robot Guide จังหวัดน่าน

โปรเจกต์ AI Guide แนะนำสถานที่ท่องเที่ยวและข้อมูลสำคัญของจังหวัดน่าน พัฒนาด้วย **Python (FastAPI)** เต็มรูปแบบ เน้นความเรียบง่ายและประสิทธิภาพ

## ✨ คุณสมบัติหลัก (Features)

*   **Chat Interface:** หน้าแชทสำหรับพูดคุย (Text & Voice) รองรับการแสดงผลแผ่นที่และรูปภาพ
*   **Avatar Interface:** หน้าหุ่นยนต์ Avatar (3D) ที่โต้ตอบได้ พร้อมระบบ **Wake Word** ("น้องน่าน")
*   **Speech System:**
    *   **STT (ฟัง):** รองรับ Whisper (Local/Cloud)
    *   **TTS (พูด):** Streaming EdgeTTS (เสียงธรรมชาติ) ตอบกลับทันที
*   **Admin Panel:** หน้าจัดการข้อมูลสถานที่และสินค้า เชื่อมต่อ Google Sheets
*   **RAG Pipeline:** ระบบค้นหาข้อมูลอัจฉริยะจาก Vector DB (Qdrant)
*   **Multi-language:** รองรับ 7 ภาษา (ไทย, อังกฤษ, จีน, ญี่ปุ่น, ฯลฯ)

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

*   **Frontend:** HTML5, CSS3, JavaScript (Vanilla JS)
*   **Backend:** Python **FastAPI** (Port 9090) - หัวใจหลักของระบบ
*   **Database:**
    *   **MongoDB** (Data Storage)
    *   **Qdrant** (Vector Search)
*   **AI Models:** Gemini / Groq (Llama 3) / LangChain

---

## 🚀 วิธีการรันโปรเจกต์ (Quick Start)

### 1. Prerequisite (สิ่งที่ต้องมี)
*   **Python 3.11+**
*   **Docker Desktop** (สำหรับ Database)

### 2. รันแบบง่าย (Recommended)
เปิด Terminal แล้วพิมพ์คำสั่ง:

```bash
# รันทุกอย่างในคำสั่งเดียว
./start_all.sh
```

ระบบจะเริ่มทำงานที่:
👉 **http://localhost:9090**

---

### 3. การรันแบบ Manual

1.  **เปิด Database:**
    ```bash
    docker-compose up -d
    ```

2.  **รัน Backend:**
    ```bash
    cd Back-end
    # (Activate venv ก่อน)
    python -m uvicorn api.main:app --host 0.0.0.0 --port 9090 --reload
    ```

3.  **เข้าใช้งาน:** เปิด Browser ไปที่ `http://localhost:9090`
